Run on command line with python3 acidtrip.py, runs on http://127.0.0.1:5000
Place image in the input folder. Generated image will appear in output folder
The closer the precision, the more likely the image will look like the original (Precision of 1 will output the exact image).
Single and multiple trips work.
Acceptable image types: .jpg, .jpeg, .png, .webp